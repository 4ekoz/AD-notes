- hostname+domain name =FQDN (fully qualified domain name).
- server name is pdc and the domain called cita.local --> FQDN = pdc.cita.local.
- DNS in network don't know reach to pc with pdc  ,, u must set to it FQDN .
# Prefix 
- hostname prefix -->  in the world of AD ,, THE name that comes first  called prefix --> pdc.
- network prefix --> if we talk about ips ex : 192.168.1.0/24 ,, prefix is 192.168.1 .


# suffix 
- that was the part the coming at the end . 
- DNS suffix --> the domain that pc member of it --> cita.local.
- when you are in Windows 7, you start working with ping PDC (without writing the full name), Windows goes or automatically loads its extension (which is cita.local) and tries to connect the devices.