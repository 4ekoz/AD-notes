- it is the main database for AD .
-  By default, it is located at `%SystemRoot%\NTDS\Ntds.dit` 
- In your VM lab (Windows Server 2019), you don't "apply" NTDS like a setting; it is created automatically when you promote the server to a **Domain Controller**. However, you can manage and move it  ,,, When you ran the _Active Directory Domain Services Configuration Wizard_, there was a page called **Paths**. This is where you chose where the NTDS database and logs would live.



1- domain partitions  ( store users , groups , computer , password hashed ) . --> ad users and computers .

![[Pasted image 20260221071759.png]]
2-instructions  partition (Ad sites and services)  --> describe the physical structure of network .
control of : 
2.1-how domain controller (DC) communicate . 
2.2-how replication happens .
2.3-which domain controller connected to .
2.4-how AD traffic optimized across network .

3-schema partitions (general shape of AD and divided to classes and attributes) .
4-application partition (open by dns console)


operation master rule : 
1-domain naming master role .
2-schema master role .
3-RID (relative id )--> device that responsible for distribute sid .
4-pdc (primary domain controller) --> server responsible for security transactions .
5-GC (global catalog) --> a server that have all infos about all objects all over the domain .
6- infrastructure--> user can search about any object in another domain .







# what is sson (single sign on ) ? 
once u entered u computer and authenticate (username&pass)  on AD ,, AD send tickets and set permissions  to users account .